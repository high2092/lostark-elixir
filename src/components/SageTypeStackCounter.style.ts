import styled from '@emotion/styled';
import { STACK_COUNTER_EXPECTED_HEIGHT } from '../constants';

export const SageTypeStackCounter = styled.div`
  height: ${STACK_COUNTER_EXPECTED_HEIGHT};
  padding: 0.5vw 0;
  * {
    padding: 0 0.5vw;
  }
`;
