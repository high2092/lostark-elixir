import styled from '@emotion/styled';

export const Footer = styled.div`
  position: fixed;
  right: 0;
  bottom: 0;

  font-family: 'Poor Story', cursive;
  font-size: 0.8rem;

  display: flex;

  div {
    padding: 0 0.1rem;
  }
`;
